import React from "react";

const AppContext = React.createContext({})

export default AppContext; 
//despues lo vamos a utilizar e inicializar este context con la informacion que necesite
//conectemos el context con la aplicacion (App.jsx)